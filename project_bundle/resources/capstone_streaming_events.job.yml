resources:
  jobs:
    capstone_streaming_events:
      name: ${var.env}_capstone_streaming_events_${workspace.current_user.short_name}
      tasks:
        - task_key: streaming_autoloader
          notebook_task:
            notebook_path: /Workspace/Users/${workspace.current_user.userName}/eb-databricks-for-de-capstone/instructors/notebooks/2-streaming-autoloader
            base_parameters:
              catalog: ${var.target_catalog}
              env: ${var.env}
            source: WORKSPACE
          libraries:
          - pypi:
              package: pytest==8.4.2
          existing_cluster_id: ${resources.clusters.personal_cluster.id}
